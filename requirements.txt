# 智识库知识平台 · 核心依赖清单（requirements.txt）
#
# 这里是「平台能跑起来 + 日常功能全部可用」所需的最小集合：
# 知识库 / WiKI / 书架 / 办公平台 / 自媒体 / 知识收集 均在此范围内。
#
# 安装：pip install -r requirements.txt
#（用「一键安装.cmd」会自动完成这一步）
#
# 视频转图文 / 图片 OCR / 语音转写 等重型能力拆到了
# requirements-optional.txt，避免全新安装时被几 GB 的 PyTorch 拖垮。

# --- Web 框架 ---
Django==5.2
asgiref==3.12.1
sqlparse==0.6.0
tzdata==2026.4              # Windows 必需：系统没有时区数据库

# --- 后台 / 编辑器 / 导入导出（三者都在 INSTALLED_APPS 里）---
django-simpleui==2026.1.13
django-mdeditor==0.1.20
django-import-export==4.4.1

# --- Markdown 渲染（core/services.py）---
Markdown==3.10.3

# --- 本地导入管道：文档解析 ---
Pillow==11.3.0
PyPDF2==3.0.1
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5

# --- 网页解析管道 ---
lxml==6.1.3
beautifulsoup4==4.15.0
readability-lxml==0.9
cssselect==1.3.0
requests==2.34.2

# --- 其他：Gitee 仓库状态 / AI 服务调用 ---
GitPython==3.1.62
dashscope==1.27.5
